# Session kickoff — M3: Toppling, rounds & stats

Read first: CLAUDE.md, docs/MILESTONES.md (M3 checklist), docs/PLAN.md
sections 1 + 9 (impact detector, topple detection, stats), docs/
SESSION_LOG.md.

TDD the pure core: impact detector (|Δv| heuristic — remember: NO
collision-event API exists, per DECISIONS), core/topple.ts (tilt >
config threshold + at-rest ~0.5 s; wobble never counts), core/scoring.ts
round reducer (6-stick rounds, result, auto-reset), core/stats.ts
(PBs incl. longest throw + longest felling throw, lifetime totals incl.
play time + accuracy, versioned zod schema with reserved userId/matches
fields; persistence as JSON). All driven by core/events.ts — nothing
counted in UI code. HUD shows round counts + the relevant PB. Verify
end-to-end in the emulator via scripted MCP throws (throw → kubb falls →
score updates → round ends → reset). Short plan first; review gate +
ritual per CLAUDE.md.
