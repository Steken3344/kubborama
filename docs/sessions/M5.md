# Session kickoff — M5: Polish & performance

Read first: CLAUDE.md, docs/MILESTONES.md (M5 checklist), docs/PLAN.md
sections 5-6 (SFX inventory, performance budget), docs/SESSION_LOG.md.

Audio: implement the full SFX inventory (Kenney Impact Sounds — the
stick-vs-kubb KLONK is the signature sound, spend care picking it;
2-3 variants each, pitch ±10%, volume by impact force, via
AudioUtils.play/one-shot entities). Ambience: 2-3 POSITIONAL bird loops
placed in the trees + one non-positional cozy music loop (Pixabay —
log source+license in ASSETS.md; FreePD is dead, don't use it). Wire
music/SFX channels to settings. Performance: GC/pooling pass (zero
per-frame allocations), draw calls <100, then verify 72 Hz ON THE REAL
QUEST 2 — chrome-devtools-mcp over adb (requires the USB card + dev
mode). Optional: king-cam slow-mo replay on king topple. The milestone
ends at ERIK'S HEADSET GATE (perf + comfort + full-experience pass).
Short plan first; review gate + ritual per CLAUDE.md.
