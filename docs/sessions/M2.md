# Session kickoff — M2: Throwing (the most important milestone)

Read first: CLAUDE.md, docs/MILESTONES.md (M2 checklist), docs/PLAN.md
sections 1 + 9 (throw math, spin styles, ballistic bands, tuning lab),
docs/DECISIONS.md (grab/velocity API facts), docs/SESSION_LOG.md.

Throw feel is the product — do not rush this milestone. TDD the pure
core first: core/throwRelease.ts (frame-averaged release + lever-arm
term) against synthetic controller sample series. Then: end grip
(handle offset), distance grab (returnToOrigin OFF), state machine,
haptics, telemetry to JSON, the tuning lab (0-100 params, live meters
with ballistic bands, presets A/B/C, JSON export/import), underhand
classifier. Both spin styles (flat + backspin) must emerge from physics
— no assists. Script a repeatable throw via MCP (xr_animate_to underhand
arc + timed release) as the golden-throw harness. Start with a short
plan for approval. The milestone ends at ERIK'S HEADSET GATE: his
recorded flat + backspin throws become the golden JSON — do not tag M2
before that calibration. Review gate + ritual per CLAUDE.md.
