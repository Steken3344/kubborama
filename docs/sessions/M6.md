# Session kickoff — M6: PWA packaging (optional, small)

Read first: CLAUDE.md, docs/MILESTONES.md (M6 checklist),
docs/SESSION_LOG.md.

Add vite-plugin-pwa (pre-approved): web app manifest (name KubbOrama,
Swedish + English descriptions), icons (generate from a simple kubb
king motif), service worker with sensible caching (app shell + assets;
never cache-bust the deploy flow). Verify: install from the deployed
GitHub Pages URL in the Quest browser → icon appears in the Quest app
library → launches fullscreen without browser chrome. Confirm scope/
start_url work under the /kubborama/ subpath. Update README with the
"install as app" instructions. Review gate + ritual per CLAUDE.md —
then the POC is COMPLETE: write a closing summary in docs/SESSION_LOG.md
(what was built, tuning values that won, all open issues) as the
foundation for the next phase decision (features vs multiplayer vs
Godot port — see docs/GODOT_PLAN_B.txt triggers).
