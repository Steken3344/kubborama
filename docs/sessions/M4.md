# Session kickoff — M4: Wind, tunables & settings

Read first: CLAUDE.md, docs/MILESTONES.md (M4 checklist), docs/PLAN.md
sections 1 + 9 (wind rule, settings), docs/SESSION_LOG.md.

Wind: per-tick force on sticks in Flying state ONLY (re-add
PhysicsManipulation each tick), windVector + dragFactor from config.
Player settings panel (spatial UI, separate from dev panel): music/SFX
volumes, haptics toggle + intensity, language sv/en, game mode Simple
(6x3, wind 0, topple 50°) / Advanced (8x5, wind 1.5 m/s lateral, topple
60°), local profile name (first-run prompt NON-blocking — throwing works
before it's answered), stats tab, court-lines toggle. Everything through
i18n — zero hardcoded strings; verify åäöÅÄÖ render in the font atlas.
Settings persist via zod-validated JSON. Dev debug panel reuses the M2
tweakpane. Optional flourish if time allows: drifting leaf particles
(three.quarks) as wind indicator. Short plan first; review gate + ritual
per CLAUDE.md.
