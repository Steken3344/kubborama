# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

KubbOrama is a new, empty project — no source code, build tooling, or tests exist yet beyond a placeholder README. There is no established architecture to describe.

Once the codebase has real structure (source layout, build/lint/test commands, framework choices), update this file to reflect:
1. Commands for building, linting, and running tests (including running a single test).
2. High-level architecture — the "big picture" that spans multiple files or directories.
