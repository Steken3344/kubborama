import { defineComponents } from '@iwsdk/core';

// No custom components yet — M1 is scene + physics only. Throwing/
// scoring/state-machine components arrive in M2+.
export default defineComponents([]);
