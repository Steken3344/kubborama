import { createComponent } from '@iwsdk/core';

export const CourtLine = createComponent(
  'CourtLine',
  {},
  'Marks a court boundary-line piece; CourtLinesSystem toggles its visibility from the courtLinesVisible setting (docs/sessions/M4.md).',
);
