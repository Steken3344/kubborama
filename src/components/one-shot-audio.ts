import { createComponent } from '@iwsdk/core';

export const OneShotAudio = createComponent(
  'OneShotAudio',
  {},
  'Marks a transient audio-only entity for disposal once its clip finishes playing — see OneShotAudioSystem.',
);
