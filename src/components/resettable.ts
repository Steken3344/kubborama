import { createComponent } from '@iwsdk/core';

export const Resettable = createComponent(
  'Resettable',
  {},
  'Marks a kubb/king/stick as restored to its authored spawn pose by the reset menu (src/systems/menu.ts).',
);
